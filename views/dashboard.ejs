<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"/>

</head>
<body class="bg-light">

  <div class="container mt-4">

    <!-- Mensagens de sucesso/erro -->
    <%- include('partials/messages') %>

    <div class="row justify-content-center">
      <div class="col-lg-8 col-md-10 col-12">
        <div class="card shadow border-0">
          <div class="card-body p-4">

            <!-- Título da Dashboard -->
            <h3 class="text-center text-primary mb-4">
              <i class="fas fa-tachometer-alt"></i> Dashboard
            </h3>

            <!-- Conteúdo principal -->
            <div class="row text-center">

              <!-- Card de Perfil -->
              <div class="col-md-4 mb-3">
                <div class="card border-0 shadow-sm h-100">
                  <div class="card-body">
                    <i class="fas fa-user fa-2x text-primary mb-2"></i>
                    <h6 class="fw-bold">Profile</h6>
                    <p class="small text-muted mb-2">View and edit your info</p>
                    <a href="#" class="btn btn-outline-primary btn-sm">Go</a>
                  </div>
                </div>
              </div>

              <!-- Card de Configurações -->
              <div class="col-md-4 mb-3">
                <div class="card border-0 shadow-sm h-100">
                  <div class="card-body">
                    <i class="fas fa-cog fa-2x text-success mb-2"></i>
                    <h6 class="fw-bold">Settings</h6>
                    <p class="small text-muted mb-2">Manage your preferences</p>
                    <a href="#" class="btn btn-outline-success btn-sm">Go</a>
                  </div>
                </div>
              </div>

              <!-- Card de Logout -->
              <div class="col-md-4 mb-3">
                <div class="card border-0 shadow-sm h-100">
                  <div class="card-body">
                    <i class="fas fa-sign-out-alt fa-2x text-danger mb-2"></i>
                    <h6 class="fw-bold">Logout<
